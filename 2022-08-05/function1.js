//함수
//함수는 특정코드를 하나의 명령으로 실행할 수 있게 해주는 기능
// 파라미터가 주어졌을 때 파라미터를 처리해서 결과를 만든다.
// 파라미터는 인풋, 넣는 값이다.
//결과 값은 return이라는 키워드를 사용한다.

function add(a, b) {
    return a + b;
}

const sum = add(1,2);
console.log(sum);